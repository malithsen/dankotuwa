# Dankotuwa Mobile app

## Build steps

* `npm install -g cordova ionic`
* `npm install`
* `bower install`
* `ionic state reset`

## Running on browser

* `ionic serve`

## Debugging on phone

* `ionic run -c -l --debug`
